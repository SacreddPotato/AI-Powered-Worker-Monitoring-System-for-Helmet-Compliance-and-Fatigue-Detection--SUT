[pytest]
DJANGO_SETTINGS_MODULE = sentinel.settings
pythonpath =
    backend
markers =
    ml: real machine-learning model inference tests
    slow: tests that may load large model weights or run slower than unit tests
    regression: regression tests for behavior that has broken before
testpaths =
    backend/tests
timeout = 180
